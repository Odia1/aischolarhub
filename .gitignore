# Secrets — never commit real values, only .env.example
.env
setup1.txt

*~
#*

# Runtime data — bind-mounted DB storage, large binary files, not config
mongo_data/
meili_data/
pgvector_data/

# Generated at runtime, not config
uploads/
logs/
backups/

# OS cruft
.DS_Store
ollama_data/
